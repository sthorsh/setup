KOMMANDO 

Alias
  $ alias

Funksjon
  $ declare -F

Builtin kommando
  $ :
  $ bg OR bg jobspec (jobspec: %N)
  $ bind -pv OR bind keyseq:function OR bind -r keyseq OR bind -u function
  $ builtin builtin
  $ caller
  $ cd OR cd - OR cd dir 
  $ command extcmd
  $ complete OR complete -A action name                           
  $ compgen -A action [word]
  $ compopt name OR compopt ±o option name 
  $ coproc cmd
  $ declare -pF OR declare -pf name OR declare -aAinrx OR declare [-aAinrx] name[=value]
  $ disown OR disown jobspec|pid
  $ echo arg ...
  $ enable | enable -n | enable builtin | enable -n builtin
  $ eval arg ...
  $ exec extcmd OR exec redirection
  $ exit OR exit N
  $ export OR export name[=value]
  $ false
  $ fg OR fg jobspec
  $ function name { cmds; } 
  $ getopts optstring name arg
  $ hash 
  $ help OR help topic
  $ history
  $ jobs
  $ kill -l [sigspec] OR kill -sigspec jobspec|pid (sigspec: 1|hup|sighup etc)
  $ let arg ...
  $ local [-aAinrx] name[=value]
  $ logout OR logout N
  $ printf format arg ...
  $ pwd
  $ read OR read -n N OR read -u N or read var
  $ readarray OR readarray -n N OR readarray -u N OR readarray var
  $ readonly OR readonly name=value
  $ return OR return N
  $ set C-i OR set ±o opt OR set [- or --] arg ...
  $ shift OR shift N
  $ shopt OR shopt -o OR shopt -s opt OR shopt -u opt
  $ source file.sh arg ...
  $ suspend OR suspend
  $ test expr 
  $ time pipeline
  $ times
  $ trap -l OR trap cmd sigspec
  $ true
  $ type name
  $ ulimit -a OR ulimit -Ha OR ulimit -cdfilmnqstuvx N
  $ umask OR umask -S OR umask mode
  $ unalias name
  $ unset var OR unset function
  $ wait OR wait jobspec|pid

Ekstern kommando: binærfil
  $ foo

Ekstern kommando: script
  $ foo

