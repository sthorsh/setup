KUBECTL

kubectl
  $ kubectl<cr>
  $ kubectl <ht>
  $ kubectl <cmd> --help
  
kubectl <cmd>
  $ kubectl api-resources
  $ kubectl apply -f <manifest> --record
  
  $ kubectl cluster-info

  $ kubectl create -f <manifest>

  $ kubectl delete <resources>
  $ kubectl describe resources>

  $ kubectl explain <resource>

  $ kubectl get <resources>

  $ kubectrl port-forward [podname] port₁:port₂

  $ kubectl replace ...

  $ kubectl rollout <cmd> ...

  $ kubectrl run [podname] --image=nginx:alpine
                                   
