DATA

Opts
  $ set -o
  $ shopt
  
Params
  -      opts (read-only)
  $      pid bash (read-only)
  !      pid last bg job (read-only)
  ?      exit status last fg pipeline (read-only)
  #      positional param count (read-only)
  *      positional params 1... (read-only)
  @      positional params 1... (read-only)
  _      name shell OR name script OR last arg last fg cmd OR ... (read-only)
  0      name shell OR name script
  1      param 1
  ...
  {10}   param 10

Vars
  $ declare -p
  
  $ name=value
  $ name+=value
  
  $ declare -a name[=value]
  $ declare -A name[=value]
  $ declare -i name[=value]
  $ declare -n name[=value]
  $ declare -x name[=value]

  $ export name
  $ export name=value

  $ local opts name
  $ local opts name=value

  $ unset name

