init
  Lag .git
  Lag master branch
  Lag HEAD → master

--------------------------------------------------
status
  Branch
  No commits yet
  Untracked files
  Changes not staged for commit
  Changes to be committed
  Nothing to commit, working tree clean

--------------------------------------------------
add
  Add endringer til index

mv
  Move files in wtree
  Move files in index

rm  
  Remove files in wtree
  Remove files in index

----------------------
commit
  Commit til repo
  Lag ny commit
  Flytt master til ny commit
  Flytt HEAD til master (implisitt)

revert

----------------------
branch
  Alt1: list [--all | --remotes] branches
  Alt2: lag ny branch fra HEAD [el <start-point>]

switch
  Move HEAD
  Update index
  Update wtree
  
checkout
  Alt1: move HEAD; update index; update wtree (cf switch)
  Alt2 w/pathspec: update index; update wtree (cf restore)

merge
   Fast-forward: ingen ny commit
   Merge commit: ny merge commit

----------------------
rebase
  Gjenta commits on top of another base tip
  UC: alternativ til merge

reset
  Move HEAD to commit; reset index fra repo; hvis hard: reset wtree fra repo
  UC1: ...  
  UC2: ...

restore
  Restore wtree fra index
  UC1: 
  UC2: ...

