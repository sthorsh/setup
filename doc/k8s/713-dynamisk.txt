DYNAMISK

Storage e.g nfs
  On storage server: install nfs server
  On storage server: configure nfs export
  On each worker node: install nfs client
  On each worker node: test mounting export

StorageClass
  ...

PersistentVolumeClaim (pvc)
  name: pvc-nfs-data
  accessModes: rwo|rwx|rox
  resources.requests.storage: 10Gi
  resources.limits
  storageClassName: ...
  selector: key=value
                                  
  Volume
    name: webcontent
    persistentVolumeClaim: ...

  Node
    Kubelet mounter pv til node
  
  Pod
    Kubelet eksponerer pv i pod
    $ kubectl exec -it nginx-nfs-deployment-596984497d-nh2s6 -- /bin/bash

