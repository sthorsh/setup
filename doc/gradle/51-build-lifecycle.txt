GRADLE

Project                           application, root project
  settings.gradle                 top level settings
  build.gradle                    top level build script
  Project                         subproject
    src                           project source code
    build.gradle                  plugins, repos, deps, tasks, ...
  ...

Plugins
  Known plugins                   java, ...
  Community plugins               flyway, ...
  Plugins have tasks              may be overridden
  Plugins have properties         may be overridden

Repositories
  Public                          maven central, ...
  Local                           ~/.gradle/caches/modules-2/files-2.1/

Dependencies
  cf 41-avhengigheter.txt

Tasks
  doFirst
  doLast
  dependsOn <task>              
  
Task
  Builtin task                    init, ...
  Plugin task                     clean, ...
  Defined task                    foo

Build lifecycle
  Initialization                  once
  Configuration                   executes build scripts, builds DAG of tasks
  Execution                       executes tasks according to dependency order

Initialization
  Starting build
  Evaluates settings  
  Loads projects

Configuration
  

Execution

