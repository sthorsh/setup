INSTALL MINIKUBE

Update
  $ cd ~/install (?)
  $ sudo apt-get update
  $ sudo apt-get install -y apt-transport-https (?)

Install
  $ curl -Lo minikube https://storage.googleapis.com/minikube/releases/v0.28.2/minikube-linux-amd64
  $ chmod +x minikube && sudo mv minikube /usr/local/bin/

Start
  $ minikube start (starter virtualbox; starter k8s single-node cluster)

Dashboard
  $ minikube dashboard &

Cf
  http://matthewpalmer.net/kubernetes-app-developer/articles/install-kubernetes-ubuntu-tutorial.html

