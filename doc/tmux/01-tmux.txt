tmux
  C-a           prefix
  ?             list-keys

  d             detach-client

  $             rename-session

  c             new-window
  ,             rename-window
  C-hl          select-window
  1-9           select-window
  f             find-window content|name|title
  C-S-←→        swap-window
  C-o           rotate-window
  |             split-window
  -             split-window
  C-w           kill-window

  hjkl          select-pane
  L             pipe-pane
  C-A-s         setw synchronize-panes
  q             display-panes
  !             break-pane
  {}            swap-pane
  C-A-←↑↓→      resize-pane
  z             resize-pane -Z
  x             kill-pane
  C-A-a         kill-pane -a

  C-c           copy-mode-vi
  y             selection → tmux buffer, clipit buffer
  C-j           selection → tmux buffer
  A             selection → tmux buffer append
  Y             EOL → tmux buffer

  =             tmux buffer
  p             paste from tmux buffer
  C-v           paste from clipit buffer

  :             command-mode

  r             reload tmux.conf

  C-I           tpm: install
  C-U           tpm: update

  C-s           tmux-resurrect: save
  C-r           tmux-resurrect: restore

--------------------------------------------
1. tmux: _terminal_ multiplexer
   session: _terminal_ sesjon
   window: _terminal_vindu

2. Gul lapp: tmux → tmux new-session -s base

3. gnome-terminal-─┬─3*[bash───tmux: client] ($ tmux i.e per terminalsesjon)

4. tmux: server─┬─bash───vi
                ├─bash───sleep
                ├─bash───... (e.g $ tmux new-window e.g per terminalvindu)

