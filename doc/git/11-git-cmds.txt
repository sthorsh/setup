GIT CMDS

git
  $ git add                         mv wrk_area → stg_area
  $ git am
  $ git annotate
  $ git apply
  $ git archive
  $ git bisect
  $ git blame
  $ git branch
  $ git bundle
  $ git checkout                    switch to branch: mv HEAD && update wrk_area
  $ git cherry
  $ git cherry-pick
  $ git clean
  $ git clone                       cp loc_repo ← rem_repo && build wrk_area
  $ git commit                      mv stg_area → loc_repo
  $ git config
  $ git describe
  $ git diff
  $ git difftool
  $ git fetch <remote>              loc_repo ← rem_repo
  $ git filter-branch
  $ git format-patch
  $ git fsck
  $ git gc
  $ git get-tar-commit-id
  $ git grep
  $ git help
  $ git imap-send
  $ git init                        mv wrk_area → loc_repo
  $ git instaweb
  $ git interpret-trailers
  $ git log
  $ git merge <src> <dst>           merge source branch into destination branch
  $ git mergetool
  $ git mv
  $ git name-rev
  $ git notes
  $ git pull <remote>               fetch + merge
  $ git push <remote> <branch>      loc_repo → rem_repo: push commits to branch
  $ git rebase
  $ git reflog
  $ git remote
  $ git repack
  $ git replace
  $ git request-pull
  $ git reset
  $ git revert
  $ git rm
  $ git shortlog
  $ git show
  $ git show-branch
  $ git stage
  $ git stash
  $ git status
  $ git submodule
  $ git subtree
  $ git tag
  $ git verify-commit
  $ git whatchanged
  $ git workspacetree

