MINIKUBE

$ minikube
  $ minikube<cr>
  $ minikube <ht>
  $ minikube <cmd> --help
  ...

$ minikube <cmd>
  $ minikube addons
  $ minikube cache
  $ minikube completion
  $ minikube config
✓ $ minikube dashboard
  $ minikube delete
  $ minikube docker-env
  $ minikube get-k8s-versio
✓ $ minikube ip
  $ minikube logs
  $ minikube mount
  $ minikube profile
  $ minikube service
✓ $ minikube ssh
  $ minikube ssh-key
✓ $ minikube start
  $ minikube status
  $ minikube stop
  $ minikube update-check
  $ minikube update-context
  $ minikube version

