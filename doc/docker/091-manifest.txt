MANIFEST

$ docker manifest
  annotate <manifest_list> <manifest>    add information to local image manifest
  create <manifest_list> <manifest>      create local manifest list 
  inspect <manifest>                     display image manifest or manifest list
  push <manifest_list>                   push manifest list to a repository
