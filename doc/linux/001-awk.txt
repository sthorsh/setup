AWK

Awk
  awk [opts] 'program'
  awk [opts] 'program' file₁ …
  awk [opts] 'program' var=val file₁ …
  … 
  
Opts
  --assign var=val (-v)
  --dump-variables (-d)
  --field-separator:fs (-F)
  --pretty-print (-o)
  …

Program
  pattern { stmt; … }
  pattern { stmt; … }
  …
  function name(args) { … }
  
Pattern
  BEGIN
  BEGINFILE
  Empty
  Expression: exp
  Regular expression: /regexp/
  Record range: begin-pattern, end-pattern
  ENDFILE
  END

AWK STATEMENTS

Input
  getline

Output
  print item₁, item₂, … [> "filename"]
  printf "", item₁, item₂, … [> "filename"]

Delete
  delete

Kontroll
  if-else
  while
  do-while
  for(e in arr) {...}
  for(expr; expr; expr) {...}
  switch
  break
  continue
  next
  nextfile
  exit

Compound
  {...}

Uttrykk
  Funksjonskall → 
  Tilordning


---------------
Types
  Number
  String
  

