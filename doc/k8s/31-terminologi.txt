TERMINOLOGI

K8s
  When you deploy k8s, you get a cluster (k8s)
  If you’re running k8s, you’re running a cluster (redhat)

Cluster
  Master node(r)
  Worker node(r)
  ...

Tilstand
  Desired state: manifest
  Current state: drift

Objekt
  Persistent entitet
  Representerer tilstand
  Pod: pod
  ReplicaSet: pods + desired state
  Service: aksess til pods + LB
  Endpoint: list of all pods in service
  Deployment
  Job

Controller
  Overvåker tilstand
  Regulerer tilstand (termostat)
  DeploymentController, ReplicaSetController, JobController

-------------------
Label
  Produksjon: prod
  Backend: be
  Versjon: 1.2.3

