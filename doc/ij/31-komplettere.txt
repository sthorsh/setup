KOMPLETTERE

  [ctrl-space]               
    Nøkkelord         prefix|
    Navn              pvfmC₀₁₂
    Nytt navn         T |

  ctrl-space x 2
    Navn              static fm₀₁₂

  ctrl-space x 3
    Navn              C₃

  ctrl-shift-space    
    Navn              pvfm, new |, static T T.fm₀₁₂
    Casting           T t = (|)
    Generic           T<S> t = new T<|>()
    Lambdas           stream.reduce(|)
    Builder           DoubleStream ds = |
    Delegering        ref.m(|) → ref.m(params)
    Overriding        super.m(|) → super.m(params)

  ctrl-shift-space    
    Navn              static T S.fm₀₁₂
    Referanser        ref.fm₀₁₂
    Array             a[...], Arrays.asList(a)
    Collection        c.get(i), c.toArray(new T[0])

  ctrl-shift-space    
    Referanser        ref.fm for String

  ctrl-alt-shift-space
    Strenger          prefix|

  ctrl-shift-enter    
    Metode            T m(|)
    Setning           int x|= 123
    Setning           if|, for| mfl

  Terminator          ⇥ ! ␣ . , = ; ctrl-shift-↵ 

