EXPR

Opsjoner
  -o set-opt              on?

Params
  -n $param               not empty?
  -z $param               zero? 
  $param = word           er lik word?
  $param < word           alfabetisk før?
  $param > word           alfabetisk etter?
    $param == pattern       [[]]: lik pattern?
    $param != pattern       [[]]: ulik pattern?
    $param =~ regex         [[]]: lik regex?

  Variabler
    -v var                  deklarert?
    -R var                  referanse?

  Aritmetisk
    N₁ -eq N₂               equal?
    N₁ -ne N₂               not-equal?
    N₁ -lt N₂               less?
    N₁ -gt N₂               greater?
    N₁ -le N₂               less-or-equal?
    N₁ -ge N₂               greater-or-equal?

  Filer
    -b fil                  block device?
    -c fil                  char device?
    -d fil                  directory?
    -e fil                  eksisterer?
    -f fil                  eksisterer og regular fil?
    -g fil                  setgid bit?
    -G fil                  eid av min gruppe?
    -h fil                  symbolsk lenke?
    -k fil                  sticky bit?
    -N fil                  endret etter sist lest?
    -O fil                  eid av meg?
    -p fil                  named pipe?
    -r fil                  readable for meg?
    -s fil                  eksisterer og ikke tom?
    -t fildeskriptor        åpen i en terminal?
    -u fil                  setuid bit?              
    -w fil                  writeable for meg?
    -x fil                  executable for meg?
    fil₁ -ef fil₂           lenke til?
    fil₁ -nt fil₂           nyere enn?
    fil₁ -ot fil₂           eldre enn?

  Logisk
    ! e                     not
    e₁ -a e₂                test-OR-[]: and
    e₁ -o e₂                test-OR-[]: or
    e₁ && e₂                [[]]: and
    e₁ || e₂                [[]]: or
   
--------
Kontekst
  test expr OR [ expr ]
  [[ expr ]]
