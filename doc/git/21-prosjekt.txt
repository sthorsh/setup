PROSJEKT

Prosjekt
  Working tree
  .git/

Areas
  Working tree (tree)
  Index
  Repository
  Stash: midlertidig lagring

Tree
  Prosjektkatalogen
  $ git diff → diff(tree, index)
  Clean tree: tree eq index eq repo
  
Index
  Mellomliggende lagring (staging)
  $ git diff --cached → diff(index, repo)
  Cf .git: index
  
Repository
  Prosjekthistorikken
  Cf .git: objektdb, refs

.git/
  objects/                    objektdb
  refs/heads/                 branches
  refs/heads/master           lokal master branch
  refs/remotes/origin         default remote
  refs/remotes/origin/HEAD    remote HEAD
  refs/remotes/origin/master  remote master branch (packed-ref)
  refs/tags/                  tags
  config                      remote mm
  HEAD                        lokal HEAD (singleton)
  index                       binary file

