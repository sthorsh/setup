NODER

Komponenter
  Kubelet
  Container runtime
  Kube proxy

Kubelet
  K8s agent (installert)
  Registrerer noden i clustert
  Lytter etter work assigments fra master
  Instansierer pods (→)
  Rapporterer tilbake til master
  Eksponerer /health, /pods, /spec over tcp :10255

Container runtime
  Cf docker
  
kube-proxy
  K8s networking
  For hver pod: en ip addr
  For hver container i pod: en tcp port
  For service noder: LB mellom pod noder

