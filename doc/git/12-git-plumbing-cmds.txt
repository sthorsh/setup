GIT                           

git cmd
  add                         mv wrk_area → stg_area
  am
  annotate
  apply
  archive
  bisect
  blame
  branch
  bundle
  checkout                    switch to branch: mv HEAD && update wrk_area
  cherry
  cherry-pick
  clean
  clone                       cp loc_repo ← rem_repo && build wrk_area
  commit                      mv stg_area → loc_repo
  config                 
  describe
  diff
  difftool
  fetch <remote>              loc_repo ← rem_repo
  filter-branch
  format-patch
  fsck
  gc
  get-tar-commit-id
  grep
  help
  imap-send
  init                        mv wrk_area → loc_repo
  instaweb
  interpret-trailers
  log
  merge <src> <dst>           merge source branch into destination branch
  mergetool
  mv
  name-rev
  notes
  pull <remote>               fetch + merge
  push <remote> <branch>      loc_repo → rem_repo: push commits to branch
  rebase
  reflog
  remote                  
  repack
  replace
  request-pull
  reset
  revert
  rm
  shortlog
  show
  show-branch
  stage
  stash
  status
  submodule
  subtree
  tag
  verify-commit
  whatchanged
  workspacetree
  
git plumbing cmd
  git cat-file 
  git show-ref master
