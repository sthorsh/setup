Camel
  Plassholder: {{}}
  Navn: navn/nøkkel
  Verdi: streng
  Resolve: {{navn}} → streng

Blueprint (BP)
    Plassholder: ${}
    Navn: navn/nøkkel
    Verdi: streng
    Resolve: ${navn} → streng

CLIB properties
  Statisk
  Camel plassholder for BP property: n/a
  Konstanttilordning
  Variabeltilordning
  Variabel DI bean property value
  Variabel DI BP plassholder for BP property: ${cms.uri}
  Variabel DI Camel plassholder for BP property: {{cms.uri}}
  Variabel DI Camel plassholder for BP property: {{env.cms.uri}}
        
Camel syntaks
    {{name}}
    {{name:default}}       
    {{env:NAME}} (OS)
    {{sys:name}} (JVM)

  PropertiesComponent
    setLocation("classpath:acme/clib.properties,...")
    setLocation("file:src/test/resources/clib.properties,...")
    setLocation("ref:beanID")
    setLocation("blueprint:beanID")
    ...
    
  RouteBuilder
    from|.to("{{uri}}")
    from|.to("ftp://{{env.cms.host}}:{{env.cms.port}}")
    ...

  BuilderSupport
    constant("{{name}}")
    simple("{{name}}") 
    ...

  DSL
    .bean(T.class, "{{name}}")
    .log("{{name}}") 
    .setHeader("{{name}},...)
    ...

