Opsjoner
  -o set-option         hvis on true; false

Params
  -n $s                 hvis def true; false
  -z $s                 hvis udef true; false
  $s₁ = $s₂             hvis lik true; false
  $s₁ != $s₂            hvis ulik true; false
  $s₁ < $s₂             hvis før true; false
  $s₁ < $s₂             hvis etter true; false
  ...
  ${#s}                 lengde
  ...
  ${s:offset:lengde}    delstreng
  ${s₁:-s₂}             hvis def s₁; s₂
  ${s₁:=s₂}             hvis def s₁; tilordne og s₂
  ${s₁:?s₂}             hvis def s₁; exit og s₂
  ${s₁:+s₂}             hvis def s₂; null
  ...
      ${s#pattern}          return s uten p (greedy)
      ${s##p}         return s uten p (lazy)
      ${s%p}          return s uten p
      ${s%%p}         return s uten p
      ${s/p/r}        return s med r for p
      ${s//p/r}       return s med r for p

    String extended pattern matching

  Variabler
    -v var          declared

    Aritmetiske operatorer

      arg1 + arg2
      ...
      arg1 -eq arg2   return true|false
      arg1 -ne arg2   return true|false
      arg1 -lt arg2   return true|false
      arg1 -gt arg2   return true|false
      arg1 -le arg2   return true|false
      arg1 -ge arg2   return true|false

  Filer
    -e f1           exists
    -f f1           exists && is file
    f1 -nt f2       newer than
    f1 -ot f2       older than
    ...

    Boolske op
      ! exp           true if exp is false
      exp1 -a exp2    exp1 is true and exp2 is true (ikke aritmetikk)
      exp1 -o exp2    exp1 is true or exp2 is true (ikke aritmetikk)

    Tilordning
      =   set variabelverdi
      =+  append til variabelverdi

