add
  Add endringer til index

branch
  Form 1: ls branches
  Form 2: lag branch

checkout
  Form 1: move HEAD; update index; update wtree (cf switch)
  Form 2 w/pathspec: update index; update wtree (cf restore)

commit
  Commit endringer til repo
  Lag ny commit
  Flytt master til ny commit
  Flytt HEAD til master (implisitt)

init
  Lag .git
  Lag master branch
  Lag HEAD → master

mv
  Move files in wtree
  Move files in index

reset
  Reset current HEAD: flytt current branch i repo til en annen commit
  Default: reset index fra repo
  Hard reset: reset index og wtree fra repo

restore
  Restore wtree fra index

rm  
  Remove files in wtree
  Remove files in index

status
  Branch
  No commits yet
  Untracked files
  Changes not staged for commit
  Changes to be committed
  Nothing to commit, working tree clean

switch
  Move HEAD, update index, update wtree
  
--------------------------------------------------
>> Merge
>>   Merge commit: ny commit med to parent commits (source, target)
>>   Fast-forward merge: move current branch tip to target branch tip
>>
>> Rebase
>>   Alternativ til merge
>>   Gjenta commits on top of another base tip
