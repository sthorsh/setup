KODEFULLFØRING

ctrl-space
    nøkkelord¹
    nytt navn               T t
    bruk navn               fmpv
    bruk navn               CIE@₁₂₃
    static felt             CIE@.f₁₂₃
    static metode           CE.m₁₂₃
    bruk navn³              CIE@₄
      terminator              ⇥ ␣ = ! . , ;

ctrl-shift-space
    builder¹                DoubleStream ds = |
    casting                 T t = (|)
    lambda                  stream.reduce(|)
    bruk navn               fmpv
    bruk navn               new CI₁₂₃
    static felt|metode      CI.fm₁₂₃: CI
    navn bruk               E.f₁₂₃: E
    static metode           E.m₁₂₃: E
    params                  ref.m(|) (delegat)
    params                  super.m(|)
    typeparam               T<S> t = new T<|>
    array scan²             a[...]
    array scan              Arrays.asList(a)
    collection scan         c.get(i)
    collection scan         c.toArray(new T[0])
    static felt|metode      CI.fm₁₂₃: !CI
    ref scan                ref.fm₁₂₃
    ref scan³               ref.fm: String
      terminator              ⇥ ␣ = ! . , ;

ctrl-alt-shift-space
    ord

ctrl-shift-enter
  metode                    T m(|)
  setning                   if| mfl
  setning                   int x|= 123

  ---------------------------
Synlighet
  1 - modul + jdk libs + andre libs
  2 - moduler + libs som 1 er avhengig av (avhengigheter)
  3 - moduler + libs som 2 er avhengig av (transitive avhengigheter)
  4 - moduler + libs som 1 ikke er avhengig av (uavhengigheter)

Aksess
  public
  protected
  package
  private

Idea
  Vis forslag as you type

ctrl-space && show suggestions as you type (live)
  | - n/a
  B| - n/a
  Bu| - com.oracle.nio.BufferSecrets

String
  ctrl-space: spesiell
  ctrl-shift-space: spesiell

