ROPC cf https://tools.ietf.org/html/rfc6749#section-4.3

1.1 EU oppgir akkreditiver i RP
    
2.1 RP → OP /.well-known/openid-configuration

3.1 RP sender rq til OP /token
    > grant_type=password
    > username=USERNAME
    > password=PASSWORD
    > client_id=ID
    > client_secret=SECRET
    > scope=openid profile email offline_access
    > response_type=idtoken token (Azure AD)

3.2 OP validerer rq params
    OP autentiserer RP (klientakkreditiver)
    OP autentiserer EU (brukerakkreditiver)

3.3 OP returnerer rs til RP
    < access_token: AT
    < token_type: Bearer        
    < expires_in: 3600          
    < id_token: IT

3.4 RP validerer AT cf at_hash
    RP validerer IT
  
4.1 RP sender rq til OP /userinfo
    > AT

4.2 OP validerer rq params

4.3 OP returnerer rs til RP
    < Standard|opt claims

4.4 RP validerer rs params

5.1 RP sender rq til RS /api
    > AT
    > IT

5.2 RS evt vha OP validerer AT

5.3 OP returnerer rs til RP

