APIs

Deployment
  apiVersion
  kind
  metadata
  spec: DeploymentSpec
  status

DeploymentSpec
  replicas: 1 (rs)
  minReadySeconds: 0 (rs)
  progressDeadlineSeconds: 600
  paused: false
  revisionHistoryLimit: 10
  strategy: DeploymentStrategy
  selector: LabelSelector (rs)
  template: PodTemplateSpec (rs)

DeploymentStrategy
  type: Recreate or RollingUpdate
  rollingUpdate: RollingUpdateDeployment

RollingUpdateDeployment
  maxSurge: 25%
  maxUnavailable: 25%
    
PodTemplateSpec
  metadata
  spec: PodSpec

PodSpec
  activeDeadlineSeconds
  containers: Container[]
  volumes: Volume[]
  ...

Container
  ...

