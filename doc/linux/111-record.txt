RECORD

  awk*

    cut -c list [files]
    cut -d delim -f list [files]

    join 

    sort [opts] [files]
    sort -t: /etc/passwd
    sort -t: -k4nr,4 /etc/passwd
    sort -t: -k4nr,4 -u /etc/passwd (uniq keys)
    sort -t: -k4nr,4 --stable /etc/passwd

  uniq
