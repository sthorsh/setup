HELM

helm
  $ helm create [chart]

  $ helm repo add|index|list|remove|update
  $ helm search repo|hub [keyword]
  $ helm inspect [all|readme|chart|values] [chart]
  $ helm fetch [chart]

  $ helm dependency build|list|update [chart] 

  $ helm template [chart]
  $ helm install [release] [chart] --dry-run -debug

  $ helm package [chart]

  $ helm install [release] [chart]
  $ helm upgrade [release] [chart]
  $ helm rollback [release]
  $ helm uninstall [release]

  $ helm list
  $ helm status [release]
  $ helm history [release]
  $ helm get all|hooks|manifest|notes|values [release]
  
