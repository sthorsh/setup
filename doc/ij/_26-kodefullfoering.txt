KODEFULLFØRING

ctrl-space
  nøkkelord¹
  navn dekl         T t
  navn bruk         fmpv
  navn bruk         CIE@₁₂₃
  navn bruk²        CIE@.f₁₂₃ (static)
  navn bruk         CE.m₁₂₃ (static)
  navn bruk³        CIE@₄
    terminator        ⇥ ␣ = ! . , ;

ctrl-shift-space
  builder¹          DoubleStream ds = |
  casting           T t = (|)
  lambda            stream.reduce(|)
  navn bruk         fmpv
  navn bruk         new CI₁₂₃
  navn bruk         CI.fm₁₂₃: CI (static)
  navn bruk         E.f₁₂₃: E
  navn bruk         E.m₁₂₃: E (static)
  params            ref.m(|) (delegat)
  params            super.m(|)
  typeparam         T<S> t = new T<|>
  array²            a[...]
  array             Arrays.asList(a)
  collection        c.get(i)
  collection        c.toArray(new T[0])
  navn bruk         CI.fm₁₂₃: !CI (static)
  referanser        ref.fm₁₂₃
  referanser³       ref.fm: String
    terminator        ⇥ ␣ = ! . , ;

ctrl-alt-shift-space
  ord

ctrl-shift-enter
  stmt              if| mfl
  stmt              int x|= 123
  metode            T m(|)

  ---------------------------
Synlighet
  1 - modul + jdk libs + andre libs
  2 - moduler + libs som 1 er avhengig av (avhengigheter)
  3 - moduler + libs som 2 er avhengig av (transitive avhengigheter)
  4 - moduler + libs som 1 ikke er avhengig av (uavhengigheter)

Aksess
  public
  protected
  package
  private

Idea
  Vis forslag as you type

ctrl-space && show suggestions as you type (live)
  | - n/a
  B| - n/a
  Bu| - com.oracle.nio.BufferSecrets

String
  ctrl-space: spesiell
  ctrl-shift-space: spesiell

