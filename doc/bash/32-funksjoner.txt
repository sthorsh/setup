Definition
    name() compound-command [redirections]
    function name { list; } [redirections]
    function name() compound-command [redirections]

Exit status
  Definition: 0 if name is not readonly and no syntax error
  Execution: same as last cmd if no return, otherwise return ...

