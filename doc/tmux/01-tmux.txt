tmux

  ?             list-keys
  C-a           prefix

  D             choose-client
  d             detach-client

  s             choose-tree
  $             rename-session

  c             new-window
  C-w           kill-window
  ,             rename-window
  C-hl          select-window
  1..9          select-window
  f             find-window content|...
  C-o           rotate-window
  C-S-←→        swap-window
  |-            split-window

  x             kill-pane
  C-A-a         kill-pane -a
  q             display-panes
  hjkl          select-pane
  !             break-pane
  z             resize-pane
  C-A-←↑↓→      resize-pane
  {}            swap-pane
  L             pipe-pane
  C-A-s         setw synchronize-panes

  C-c           copy-mode-vi
  y             selection → tmux buffer, clipit buffer
  C-j           selection → tmux buffer
  A             selection → tmux buffer append
  Y             EOL → tmux buffer

  =             tmux buffer
  p             paste from tmux buffer
  C-v           paste from clipit buffer

  :             command-mode

  r             reload tmux.conf

  C-I           tpm: install
  C-U           tpm: update

  C-s           tmux-resurrect: save
  C-r           tmux-resurrect: restore

--------------------------------------------
1. tmux: _terminal_ multiplexer
   session: _terminal_ sesjon
   window: _terminal_vindu


2. gnome-terminal-─┬─3*[bash───tmux: client] ($ tmux i.e per terminalsesjon)

3. tmux: server─┬─bash───vi
                ├─bash───sleep
                ├─bash───... (e.g $ tmux new-window e.g per terminalvindu)

4. Gul lapp: tmux → tmux new-session -s base

5. tmux new-session -s prod

