DSL
  ognl(): OgnlExpression

Variabler
  #this
  context
  exchange              
  exchangeId            
  property(name)        
  property(name, type)  
  properties            
  exception             
  request               
  response              

Operatorer
  {e₁, ...}
  #{e₁:e₂, ...}
  @class@static_field
  @class@static_method()
  e₁.{e₂}
  e₁.{? e₂}
  e₁ in e₂
  e₁ not in e₂
  Samt alle Java-operatorene
  
