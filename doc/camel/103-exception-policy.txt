EXCEPTION

OnExceptionDefinition
  .maximumRedeliveries(int)   resende før utmattelse
  .retryAttemptedLogLevel()   resende før utmattelse
         
  .continued(true)            ignorere 
                
  .handled(true)              catch
  .bean(...)                  route to bean (ref ProcessorDefinition)
          
  .handled(true)              catch
  .process(Processor)         route to Processor (ref ProcessorDefinition)
                
  .handled(true)              catch
  .to(String uri)             reroute to endpoint (ref ProcessorDefinition)
          

onExceptionOccurred()

onRedeliver()

onWhen()

retryWhile()

