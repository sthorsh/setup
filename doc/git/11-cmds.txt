init
  Lag .git; lag HEAD → ?

status
  Branch
  No commits yet
  Untracked files
  Changes not staged for commit
  Changes to be committed
  Nothing to commit, working tree clean
--------------------------------------------------
add
  Add til index

mv
  Move in wtree; move in index

rm  
  Remove from wtree; remove from index

commit
  Lag ny commit; flytt branch; flytt HEAD

revert

----------------------
branch
  F1: list [--all|--remotes] branches
  F2: lag ny branch fra HEAD [el <start-point>]

switch
  Move HEAD to branch; update index; update wtree
  
checkout
  F1: update index; update wtree (path)
  F2: move HEAD to <branch>; update index; update wtree (no path)

merge
   Fast-forward merge: ingen ny commit
   Rekursiv merge: _en_ ny merge commit

----------------------
rebase
  Rebase current branch oppå en annen branch

reset
  F1: reset index for <path>
  F2: reset HEAD (soft); reset index (mixed); reset wtree (hard)

restore
  Restore wtree fra index

