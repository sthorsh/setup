AWK

Syntaks
  awk opts 'pattern { stmt; ... }; ...' var=val file ...

Opts
  awk --help

Vars
  awkdv

Array vars
  arr[key] = value

Funksjoner
  awkdf

Uttrykk
  1 01 0x1 1.1 1.1e1          konstant number
  "foo"                       konstant string
  foo                         variabel
  foo[exp]                    arrayref
  foo(...)                    funksjonskall
  ()                          presedens
  $                           feltref
  ++ --                       inkrement, dekrement
  ^ **                        eksponent
  + - !                       unær pluss, unær minus, not
  * / %                       gange, dele, rest
  + -                         pluss, minus
  ∅                           konkatenere
  | |&                        omdirigere
  < <= == != > >= >>          sammenligne
  ~ !~                        sammenligne: /regexp/ ≡ $0 ~ /regexp/
  in                          medlem i array
  &&                          og
  ||                          eller
  ?:                          ternær
  = += -= *= /= %= ^= **=     tilordne

Patterns
  Tomt
  Uttrykk
  BEGIN[FILE]
  END[FILE]
  pattern₁, pattern₂

Setninger
  if(exp) stmt; else stmt
  switch(exp) { case konstant: stmt ...; default: stmt }
  for(exp; exp; exp) stmt
  for(key in arr) stmt
  while(exp) stmt
  do stmt while(exp)
  break
  continue
  delete arr[key]
  exit
  getline
  next
  nextfile
  print exp, ... > "file"
  printf "...", exp, ... > "file"
  { stmt; ...}
