Pattern
  BEGIN
  BEGINFILE
  expression
  pattern, pattern: on, off, on, off ...
  ENDFILE
  END

Actions (stmts)
  getline
  getline var
  getline < file
  getline var < file

  tilordning (exp/stmt)
  funksjonskall (exp/stmt)

    if(exp) stmt; else stmt
    switch(exp) { case konstant: stmt ...; default: stmt }
    for(exp; exp; exp) stmt
    for(key in assoc_arr) stmt
    while(exp) stmt
    do stmt while(exp)
    break
    continue
    next
    nextfile
    exit

    delete assoc_arr[key]

  print
  print ""
  print exp, ...
  print ... > file
  print ... >> file
  print ... | command
  print ... |& command
  printf fmtstr, exp, ... [redir]

    { stmt; ...}

