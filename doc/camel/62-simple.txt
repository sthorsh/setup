Syntaks
  null
  ...
  ${...}
  ${...} op ${...}

Variabler
  camelId
  routeId
  exchange (2.16)
  exchangeId
  exchangeProperty.name
  exchangeProperty.map[key]
  exception
  exception.message
  exception.stacktrace
  id
  header.name
  header.map[key]
  headers
  body
  out.body
  out.header.name
  messageHistory (2.17)
  null
  threadName
  file:absolute[.path]
  file:ext|length|modified
  file:name[.ext|.noext][.single]
  file:onlyname[.noext][.single]
  file:parent|path|size
  sys.name
  sysenv.name

  Funksjoner
  bean:beanId.methodName
  bodyAs(type)
  - collate(group) (2.17)
  date:command:pattern
  - headerAs(key,type)
  mandatoryBodyAs(type)
  - properties-location:locations:key
  - properties:key:default
  random(min,max)
  ref:beanId
  - skip(number) (2.19)
  - type:name.field

  OGNL: kun metoder
  - camelcontext.ognl
  - exchange.ognl (2.16)
  - exchangeProperty.foo.ognl
  - exception.ognl
  - header.foo.ognl
  - body.ognl
  - bodyAs(type).ognl (2.18)
  - mandatoryBodyAs(type).ognl (2.18)

  Operatorer
  - ++ --
  - == =~ < <= > >= !=
  - [not] contains 
  - [not] in 
  - [not] is 
  - [not] range 
  - [not] regex 
  - starts|ends with
  - && dvs expr && expr
  - || dvs expr || expr

