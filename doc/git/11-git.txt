GIT

git help
git help CMD

-------------------
git config 
git config --list

-------------------
git init [DIR]

git clone REPO [DIR]

-------------------
git status
git status --short
  
git log -5 
git log --all 
git log --graph 
git log --oneline
git log BRANCH 

git reflog

git diff                            unstaged changes
git diff --cached                   staged changes
git diff HEAD                       unstaged + staged changes
git diff COMMIT                     diff wtree commit
git diff --cached COMMIT            diff index commit
git diff COMMIT1 COMMIT2            diff commits
git diff BRANCH1 BRANCH2            diff branch tips
git diff BRANCH...master            diff base master_tip
git diff BRANCH1 BRANCH2 FILE       diff in file on branches

-------------------
git add PATH

git mv FILE₁ FILE₂

git rm FILES
git rm FILES --cached

-------------------
git commit -am ...

-------------------
git branch -a -v                    show
git branch b1                       create
git branch -m old new               rename
git branch -d b1                    delete
                                    
git checkout -b b1                  create-checkout
git checkout TARGET                 checkout target branch
git checkout COMMIT                 checkout commit → detached HEAD
git checkout --track REPO/REF       start tracking remote branch                                    
        
git switch b1                       
                                    
git merge SOURCE -m "..."           do ff-merge or create merge-commit
git merge --abort                   abort merge
                                    
git rebase master                   mv HEAD to master, cp/replay commits, mv HEAD, mv b1
git rebase -i master                pick, squash etc

git cherry-pick COMMIT

-------------------
git reset --soft COMMIT
git reset --hard COMMIT
git reset --mixed COMMIT

-------------------                 
git tag                             show  
git tag t1                          create
git tag -a t1 -m "."                create
git tag -d t1                       delete
                             
-------------------
git remote -v
git remote add NAME URL
  
git fetch

git pull --no-rebase
git pull --ff
git pull --rebase

git push REPO REF
git push -u REPO REF                push new branch → merge/pull request url
git push -d REPO REF                delete remote branch
  
-------------------
git stash list
git stash show
git stash
