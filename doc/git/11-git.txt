GIT 

git
  add                     workspace → staging
  am
  annotate
  apply
  archive
  bisect
  blame
  branch
  bundle
  checkout                switch to branch: move HEAD && update workspace
  cherry
  cherry-pick
  clean
  clone                   workspace ← remote repo
  commit                  staging → repo
  config                 
  describe
  diff
  difftool
  fetch                   repo ← remote repo
  filter-branch
  format-patch
  fsck
  gc
  get-tar-commit-id
  grep
  help
  imap-send
  init                    workspace → repo
  instaweb
  interpret-trailers
  log
  merge
  mergetool
  mv
  name-rev
  notes
  pull                    workspace ← remote repo
  push                    repo → remote repo
  rebase
  reflog
  remote                  
  repack
  replace
  request-pull
  reset
  revert
  rm
  shortlog
  show
  show-branch
  stage
  stash
  status
  submodule
  subtree
  tag
  verify-commit
  whatchanged
  workspacetree
  
