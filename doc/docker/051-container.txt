Container
  create <image>                    ny container
  start cnt ...                     start container; run CMD [el cmd] (spinne opp)

  run img [cmd]                     search; pull; create; start (spinn opp)
  run -dit ...                      detached interactive terminal
  run -p p₀:p₁ ...                  host_port p₀ : container_port p₁
  run -P ...                        random host ports → exposed container ports
  run -v d₀:d₁ ...                  host_dir d₀ : container_dir d₁
  run --mount src=v,target=d ...    host volume v, container directory d
  run --rm ...                      remove

  pause cnt ...
  unpause cnt ...
  restart cnt ...
  stop cnt ...
  kill cnt ...
  prune
  rm cnt ...                        remove container

  attach cnt                        attach to running container
  comnmit
  cp src dest                       path/container:path → container:path/path
  exec [-it] cnt cmd                exec_cmd_in [or exec_into] container
  export cnt -o cnt.tar             export til tar
  rename cnt new_name
  update opts cnt ...               cf stats
  wait cnt ...                      wait for running container

  ls -a                             containers
  stats                             containers statistics
  diff <container>                  diff cow_fs ro_fs
  inspect cnt                       container details
  logs [-f] <container>             PID 1 stdout/stderr
  port cnt                          container port mappings
  top cnt                           container process(es) (single-process)

