GIT

git help
git help CMD

-------------------
git config 
git config --list

-------------------
git init [DIR]

git clone REPO [DIR]

-------------------
git status
git status --short
  
git diff
git diff --staged
git diff COMMIT2 COMMIT2
git diff BRANCH1 BRANCH2

git log -5 --all --graph --oneline
git log BRANCH --oneline

git reflog

-------------------
git add PATH

git mv FILE₁ FILE₂

git rm FILES
git rm FILES --cached

-------------------
git commit -m ...
git commit -a -m ...

-------------------
git branch -a -v             show
git branch b1                create
git branch -m old new        rename
git branch -d b1             delete
                             
git checkout -b b1           create-checkout
git checkout TARGET          checkout target branch
git checkout COMMIT          checkout commit → detached HEAD
                             
git switch b1                
                             
git merge SOURCE             cp/replay commit, mv HEAD/master
                             
git rebase master            mv HEAD to master, cp/replay commits, mv HEAD, mv b1
git rebase -i master         pick, squash etc

git cherry-pick COMMIT

-------------------
git reset --soft COMMIT
git reset --hard COMMIT
git reset --mixed COMMIT

-------------------          
git tag                      show  
git tag t1                   create
git tag -a t1 -m "."         create
git tag -d t1                delete
                             
-------------------
git remote -v
git remote add NAME URL
  
git fetch

git pull
git pull --ff
git pull --no-rebase
git pull --rebase

git push [origin] [master]
git push --set-upstream origin b1
git push origin --delete b1
  
-------------------
git stash list
git stash show
git stash
