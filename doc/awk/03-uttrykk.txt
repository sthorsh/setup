Uttrykk
  11 1.1 1e1 01 0x1         num literal
  "verdi"                   str literal
  /regexp/                  regexp literal, eq $0 ~ /regexp/

  navn                      var (lval)
  navn[key]                 arrayelement (lval)
  navn(...)                 funksjonskall (exp/stmt)

  (...)                     operatorpresedens

    $exp                      feltref op (lval)
    ++lval                    preinkr op
    lval++                    postinkr op
    --lval                    predekr op
    lval--                    postdekr op
    ^                         aritmetisk op
    + - !                     artimetisk op, logisk op
    * / %                     aritmetisk op
    + -                       aritmetisk op
    exp exp ...               strengkonkatenering
    | |&                      redirigering op
    < <= == != > >= >>        sammenligning op
    exp ~ exp                 matching op, e.g exp ~ /regexp/ (EREs)
    exp !~ exp                nonmatching op, e.g exp !~ /regexp/ (EREs)
    var in array              array membership op
    && ||                     logisk op
    ?:                        conditional op
    lvalue =  rvalue          tilordning op (exp/stmt)
    lvalue += rvalue          tilordning op
    lvalue -= rvalue          tilordning op
    lvalue *= rvalue          tilordning op
    lvalue /= rvalue          tilordning op
    lvalue %= rvalue          tilordning op
    lvalue ^= rvalue          tilordning op
                                         
