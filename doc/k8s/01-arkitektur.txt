ARKITEKTUR

Applikasjon 
  FE: pod (prod, fe, 1.2.3, ...)
  FE: pod (prod, fe, 1.2.3, ...)
  LB: pod (prod, fe, 1.2.3, ...)
  IAM: pod prod, fe, 1.2.3, ...)
  Search: pod (prod, mw, 1.2.3, ...)
  DB → pod (prod, be, 1.2.3, ...)
  KV → pod (prod, be, 1.2.3, ...)
  Log → pod (prod, util, 1.2.3, ...)

K8s CLI
  $ kubectl ...

K8s API
  Deployment
  Replication controllers
  Pods
  Services
  
K8s manifest
  Desired state: ønsket tilstand for clusteret
  Current state: faktisk tilstand for clusteret
  Drift: avvik mellom desired og current tilstand
  Rectify: ta cluster tilbake til ønsket tilstand

K8s cluster
  Master
  Noder

K8s label
  Produksjon: prod
  Backend: be
  Versjon: 1.2.3

K8s stack
  K8s
  Linux
  Metall, VM, sky, openstack, ...
